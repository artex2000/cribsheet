Memory references:
Intel syntax                                GNU ASM syntax
section:[base + index * scale + disp]       section:disp(base, index, scale)

[ebp - 4]                                   -4(%ebp)
[foo + eax * 4]                             foo(, %eax, 4)  //default section is %ds
[foo]                                       foo(, 1) or foo
gs:foo                                      %gs:foo

mov bx, offset foo                          movw $foo, %bx

ORG 10h                                     .org 0x10

xxx EQU yyy                                 .equ xxx, yyy

$                                           no location pointer
string db 'this is the string'              string:
string_size EQU $ - string                      .asciiz "this is the string"
                                            string_end:
                                            .equ string_size, string_end - string
                                            
mov ax, value                               movw $value, %ax
mov ax, word ptr value                      movw value, %ax
                                            
jmp near  eax                               jmp *%eax
jmp near [100]                              jmp *100
jmp near [eax]                              jmp *(%eax)
jmp far [eax]                               ljmp *(%eax)      

-------
objcopy -j .text -O binary input.obj output.com - will generate ".com"-style binary


local labels in macros
.macro unique_labels_x3 dest
   LOCAL lbl                     ; Unique label is generated by the assembler.
                                 ; (It will start with "L", so is omitted from
                                 ; the symbol table. Can be put into symbol
lbl:                             ; table, using .global )

lbl_\@:                          ; Label generation from macro count.

ulx_\dest:                       ; Label generation from an argument.
   jmp \dest
.endm

Usage:
   unique_labels_x3 alpha        ; Labels .LL0001 - 3, lbl_0 - 2, &
   unique_labels_x3 beta         ; ulx_alpha, ulx_beta, ulx_gamma
   unique_labels_x3 gamma        ; are generated.
